@* @using RegisistryV2.ViewModel
@model ForgotPasswordViewModel
@{
    ViewData["Title"] = "نسيت كلمة المرور";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card">
                <div class="card-header">
                    <i class="bi bi-key"></i>
                    نسيت كلمة المرور
                </div>
                <div class="card-body p-4">
                    <p class="text-muted mb-4">ادخل إيميلك وهنبعتلك رابط لإعادة تعيين كلمة المرور</p>

                    <form asp-action="ForgotPassword" method="post">
                        <div class="mb-4">
                            <label class="form-label">البريد الإلكتروني</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                                <input asp-for="Email" class="form-control" placeholder="example@email.com" />
                            </div>
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <button type="submit" class="btn btn-primary w-100 py-2">
                            <i class="bi bi-send me-2"></i>إرسال رابط التغيير
                        </button>

                        <div class="text-center mt-3">
                            <a href="/Account/Login" class="text-muted" style="font-size:0.85rem">
                                <i class="bi bi-arrow-right me-1"></i>رجوع لتسجيل الدخول
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div> *@